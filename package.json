{
  "name": "laplace-robotics",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "push-dir --dir=dist --branch=gh-pages --cleanup"
  },
  "dependencies": {
    "core-js": "^3.10.0",
    "nuxt": "^2.15.4",
    "is-svg": ">=4.2.2"
  },
  "devDependencies": {
    "@nuxtjs/vuetify": "^1.11.3",
    "push-dir": "^0.4.1"
  }
}
